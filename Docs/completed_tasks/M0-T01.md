# [已完成] M0-T01:repo 骨架 + 環境離線驗證

- 結案日期:2026-08-17
- Run log:`Docs/codex_runs/M0-T01_001_env_setup.md`
- 審查結果:通過(指揮官審查 + 使用者斷網人工驗證)

## 結案紀錄

- 使用者於斷網狀態跑 `python scripts/verify_env.py`:faces detected: 1、478 landmarks、52 blendshapes、~107 FPS(IMAGE 模式)、exit 0。
- `Docs/` 底下誤生的巢狀 `.git` 已由使用者刪除。
- 環境:Windows 11、Python 3.13.7(.venv)、mediapipe==1.0.1、numpy==2.5.2、opencv-python==5.0.0.93、pyqtgraph==0.14.0、PySide6==6.11.1。
- 模型:`models/face_landmarker.task`(3,758,596 bytes,md5 b0e7274907a1644404fef66b28dd6d85),進 repo。

## 觀察清單(帶進後續任務)

1. mediapipe 1.0.1 依賴 opencv-contrib-python,與鎖定的 opencv-python 雙包共存(皆提供 cv2)。目前實測正常;之後 cv2 出怪事,第一嫌疑是它。
2. 107 FPS 是 IMAGE 模式單圖數字。M0-T02 的 30 FPS 門檻必須在 VIDEO 模式 + 鏡頭管線下重新實測。
3. FaceLandmarker 結果可能不含逐幀信心分數,「追蹤信心」需要代理指標(見 M0-T02)。

## 原任務內容(存檔)

### 目標
今晚(08/18 前)在筆電上把環境裝好,證明斷網也能跑。這是飛機上能繼續工作的前提。

### 工程師要做的
1. 建 repo 骨架(src/facetrace 四空殼模組、models/、scripts/、Docs/codex_runs/ 與 completed_tasks/、AGENTS.md)。
2. requirements.txt:opencv-python、mediapipe、pyqtgraph、PySide6、numpy,全部鎖到確定版本。
3. 下載 face_landmarker.task 模型檔進 models/,repo clone 下來就有。
4. scripts/verify_env.py:讀模型,對內建測試圖跑 Face Landmarker,印 blendshape 前十項與 FPS 估計。
5. 寫 run log 到 Docs/codex_runs/M0-T01_001_env_setup.md。

### 不准碰的
- 不寫任何 UI 程式。不實作訊號邏輯。空殼檔案只放 docstring。

### 完成定義(使用者人工驗證)
- 關掉 Wi-Fi,跑 `python scripts/verify_env.py`,看到 blendshape 數值輸出。✅ 2026-08-17 驗證通過
